TODOs:
- learning rate batchsize dependend
- Wandb logging
- Online Evaluator for comparison
- freeze last layer in first epoch
- losses => micro vs macro?
- batch structure in test/valid_step()
- how to freeze correctly in LinearProbe
- track distance between parameters over time
- initial probe is not logged?
- experiment setup:
  - create logging name and directory
  - integrate with wandb
  - store config to wandb
- schedule: 
  - copy for parameter location?
  - Schedule.__repr__(self, ) instead of super(CatSched, self)
